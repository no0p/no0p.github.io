sudo mkfs.btrfs -f -d raid0 /dev/sdc /dev/sdb

sudo mkdir /storage
sudo mount /dev/sdb /storage
sudo chown -R postgres /storage/

------
https://github.com/gregs1104/pgbench-tools
git clone git@github.com:gregs1104/pgbench-tools.git
sudo apt-get install python-pip
sudo pip install python-dateutil
-----


createdb pgbench
createdb results

psql -d template1
create tablespace bigdata location '/storage';
alter database pgbench set tablespace bigdata;
\l+

psql -f init/resultdb.sql -d results

./newset 'setup 1'

# Update config
SCRIPT="tpc-b.sql"

./runset



sudo mount -o compress=zlib /dev/sdb /storage
